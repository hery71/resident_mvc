INSERT INTO meal_tbl (meal, enabled, allergene, intolerance, ingredients)
SELECT ml.meal, ml.enabled, ml.allergene, ml.intolerance, ml.ingredients
FROM meal_dinner_dessert ml
WHERE NOT EXISTS (
    SELECT 1 
    FROM meal_tbl mt 
    WHERE TRIM(LOWER(mt.meal)) = TRIM(LOWER(ml.meal))
);